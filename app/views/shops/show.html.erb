
<div class="header-shop">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <div class="img">
          <% if @shop.picture.exists? %>
            <%= image_tag @shop.picture.url(:medium), class: "img-responsive shop-img" %>
          <% else %>
            <%= image_tag "http://placehold.it/200x200", class: "img-responsive shop-img"%>
          <% end %>
        </div>
          <h2>
            <%= @shop.name %> <br>
          </h2>
          <p>
            <%= @shop.description %>
          </p>
          <ul class="list-unstyled list-inline">
            <li class="link">
              <% if @shop.user == current_user %>
                <%= link_to "Editer votre boutique", edit_shop_path(@shop) %>
              <% end %>
            </li>
            <li class="link">
              <% if @shop.user == current_user %>
                <%= link_to "Supprimer votre boutique", shop_path(@shop), method: :delete %>
              <% end %>
            </li>
          </ul>
      </div>
    </div>
  </div>
</div>

<div class="sommaire-shop">
  <ol class="breadcrumb">
  <li class="clickable"><a href="#pastries-shop">Produits</a></li>
  <li class="clickable"><a href="#comments">Commentaires</a></li>
</ol>
</div>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-md-8 col-lg-8">
      <div id="pastries-shop">
        <div class="header-pastries">
        <ol class="breadcrumb">
          <li><h3>SES PATISSERIES</h3></li>
          <% if @shop.user == current_user %>
            <li class="link">
              <%= link_to "Ajouter une patisserie", new_shop_pastry_path(@shop) %>
            </li>
          <% end %>
        </ol>
        </div>
        <hr class="box-shadow-separator">
        <div class="row">
          <% number = 0 %>
          <% if @shop.pastries.empty? %>
            <%= "Il n'y a aucune pâtisserie " %><strong><%= @shop.name %></strong><%= " pour le moment." %>
          <% else @shop.pastries.each do |pastry| %>
            <% number = number + 1 %>
            <div class="col-xs-12 col-sm-12 col-md-6 col-lg-4">
              <div class="animateBox">
                  <% if pastry.picture_1.exists? %>
                    <%= image_tag pastry.picture_1.url(:thumb), class: "pastry-img popover-img", :'data-content' => "#{image_tag pastry.picture_1.url(:small)}" %>
                  <% else %>
                    <%= image_tag "http://placehold.it/100x100", class: "pastry-img popover-img", :'data-content' => "#{image_tag pastry.picture_1.url}" %>
                  <% end %>
                  <% if pastry.shop.user == current_user %>
                    <div class="buttons">
                      <%= link_to edit_shop_pastry_path(pastry.shop, pastry) do %>
                        <i class="fa fa-pencil-square-o"></i>
                      <% end %>
                      <%= link_to shop_pastry_path(pastry.shop, pastry), method: :delete do %>
                        <i class="fa fa-trash-o"></i>
                      <% end %>
                    </div>
                  <% end %>
                <div class="info">
                  <span class="number">nº <%= number %></span><%=pastry.title.slice(0,1).capitalize + pastry.title.slice(1..-1)%>
                  <p><%=pastry.description.slice(0,1).capitalize + pastry.description.slice(1..-1)%></p>
                </div>
                <div class="price">
                  <%= link_to add_to_cart_pastry_path(pastry), remote: true, method: :post do %>
                      <button class="btn btn-default"><%= pastry.price_per_unit %> €</button>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-xs-10 col-xs-offset-2 col-sm-2 col-sm-offset-2 col-md-2 col-md-offset-2 col-lg-2 col-lg-offset-2">
      <div class="my-cart">
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-md-8 col-lg-8">
      <div id="comments">
        <% @shop.reviews.each do |review| %>
          <%= User.find(review.shopping_cart_id).picture %>
          <%= User.find(review.shopping_cart_id).first_name %>
          <%= User.find(review.shopping_cart_id).last_name %>
          <%= "Note:" %> <%= review.rating %>
          <%= "Commentaire:" %> <%= review.content %>
        <% end %>
        <% if @can_post_review %>
          <%= render "reviews/form" %>
        <% end %>
      </div>
    </div>
  </div>
</div>


