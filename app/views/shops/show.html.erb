<div class="container">
  <div class="row">
    <div class="col-xs-4 text-center">
      <% if @shop.picture.exists? %>
        <%= image_tag @shop.picture.url(:medium), class: "shop-img img-responsive" %>
      <% else %>
         <%= image_tag "http://placehold.it/300x300", class: "shop-img"%>
      <% end %>
      <hr>
      <% if @shop.user== current_user %>
        <%= link_to new_shop_pastry_path(@shop), {class: "btn btn-primary"} do %>
          <i class="fa fa-plus"></i>
          Ajouter une pâtisserie
        <% end %>
      <% end %>
    </div>
    <div class="col-xs-8">
      <h1>
        <%= @shop.name %>
      </h1>
      <p>
        <%= @shop.description %>
      </p>
    </div>
  </div>
</div>


<div class="container text-center">
  <div class="row">
    <h2>Pastries of <%= @shop.name%></h2>
    <% @shop.pastries.each do |pastry| %>

    <% end %>
  </div>
</div>


